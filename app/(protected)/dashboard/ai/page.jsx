import { AiCommandContent } from './components/ai-command-content';

export const metadata = {
  title: 'AI Command Center',
  description: 'Natural-language control surface (simulated until Milestone 9).',
};

export default function AiCommandPage() {
  return <AiCommandContent />;
}
